# Design-productivity 技術文書

## 目的

プロジェクトごとの**流用率・不具合対応工数・不具合対応効率**を集計・グラフ表示し、設計変更の生産性指標を算出する Streamlit アプリ。  
工数データ（merged_efforts.xlsx）と台帳データ（統合図面管理台帳.xlsx）を指番で結合して分析する。

---

## ファイル構成

```
Design-productivity/
├── app.py                    # アプリ本体（全ロジック）
├── requirements.txt          # 依存パッケージ
├── TECHNICAL.md               # 本文書
├── merged_efforts.xlsx        # デフォルト工数データ（Effort-Dashboard で生成）
├── 統合図面管理台帳.xlsx        # デフォルト台帳データ（ReuseSystem/dxf_diff_results で生成）
└── .streamlit/
    └── config.toml            # Streamlit テーマ設定
```

---

## 入力ファイル仕様

### 工数データ（merged_efforts.xlsx）

単一シート（`Sheet1`）構成の工数 Excel。全シートを結合して使用する
（現行は単一シートだが、複数シート構成に戻っても動作するよう `sheet_name=None` で読み込み concat している）。

| 使用カラム | 内容 |
|-----------|------|
| `WBS要素(代入)` | 指番（結合キー）。指番以外の値（`NaN` 等、`USER_FIELD_01="指番なし"` の行に多い）も混在するが、台帳データ側の指番との一致判定で自然に除外されるため、値そのものへの正規表現フィルタは行わない |
| `USER_FIELD_01` | 工数種別（`不具合対応` でフィルタリング） |
| `USER_FIELD_02` | 要因種別（`電気設計要因` / `サプライヤー要因` を集計。他の要因種別（技術要因・機械設計要因等）は本アプリの集計対象外） |
| `作業時間(h)` | 工数（時間単位） |

### 台帳データ（統合図面管理台帳.xlsx）

`Summary` シートを使用する。`Master` / `Work Master` シートも存在するが、
`Summary` に指番ごとの集計済み指標が既に含まれているため、本アプリはそれらを読まない
（`Work Master` の `Sashiban` 単位で `Deleted/Added/Diff/Unchanged/Total Entities` を
sum すると `Summary` の値と一致することを確認済み。重複排除・集計はデータ生成元
（ReuseSystem/dxf_diff_results）側で完結しており、本アプリで再実装する必要はない）。

| 使用カラム | 内容 |
|-----------|------|
| `指番` | 指番（結合キー） |
| `削除図形総数` / `追加図形総数` / `変更図形総数` | 図形の削除・追加・変更（削除+追加）総数 |
| `図形総数` | 指番配下の全図面の図形総数 |
| `図形変更率 [%]` | 変更図形総数 ÷ 図形総数（**fraction、0〜1 で格納**） |
| `差分ペア総数` | 親子ペアで差分が検出された図面ペア数 |
| `完全新規図面数` | 親図面を持たない完全新規図面の数 |
| `指番図面総数` | 指番配下にアップロードされた図面の総数 |
| `流用率 [%]` | 差分ペア総数 ÷ 指番図面総数（fraction） |
| `新規作成率 [%]` | 完全新規図面数 ÷ 指番図面総数（fraction） |

---

## 処理ロジック

### 処理フロー

```
[統合図面管理台帳.xlsx]              [merged_efforts.xlsx]
   Summary シート読込                    全シート concat
        ↓                                   ↓
   指番と集計済み指標を取得         USER_FIELD_01=不具合対応 でフィルタ
        ↓                          WBS要素(代入) × USER_FIELD_02 でピボット
        ↓                          電気設計要因・サプライヤー要因の時間を集計
        ↓                          不具合対応工数[h] = 電気設計要因 + サプライヤー要因
        ↓                                   ↓
        └──────────── 指番で INNER JOIN ─────┘
                          ↓
              図形変更効率[図形変更数/h] = 変更図形総数 / 不具合対応工数[h]
              図面変更作業効率[図番数/h] = 差分ペア総数 / 不具合対応工数[h]
              （不具合対応工数[h] = 0 の場合はいずれも空白）
                          ↓
              Excel出力（生産性分析シート）＋ 画面に Plotly グラフ3種
```

### 指番の結合は INNER JOIN

台帳データ（Summary）と工数データの双方に存在する指番のみを出力対象とする。
片方にしか存在しない指番は出力に含めない（台帳データにあるが不具合対応工数の記録が
ない指番、または工数データにあるが当該期間の台帳データに含まれない指番は、いずれも除外）。

結合対象の指番が1件もない場合はグラフ・表を表示せず、画面に警告を表示する。

---

## グラフ（Plotly、画面表示のみ・Excelには埋め込まない）

いずれも横軸は指番。ホバー表示は小数点以下2桁・桁区切りカンマ付きで統一している
（Plotly の既定ホバーは浮動小数点の全桁をそのまま表示するため、`hovertemplate` で
明示的にフォーマットしている。既定のy軸目盛りも大きな値では `30k` のようなSI接頭辞
表記になるため、`yaxis_tickformat=",.0f"` で桁区切りカンマ付きの通常表記に変更している）。

1. **流用率** — グループ化棒グラフ、3系列: `図形変更率[%]` / `流用率[%]` / `新規作成率[%]`
   （y軸・ホバーともにパーセント表記、`%{y:,.2f}%`）
2. **不具合対応工数** — 積み上げ棒グラフ、2系列: `電気設計要因不具合対応工数[h]` + `サプライヤー要因不具合対応工数[h]`
3. **不具合対応効率** — `図形変更効率[図形変更数/h]` と `図面変更作業効率[図番数/h]` は
   桁が大きく異なる（例: 数万 vs 数）ため、左右2つのグラフに分けて表示する

---

## 出力 Excel フォーマット

シート名: **生産性分析**（1シート、データ表のみ・グラフは含まない）

| # | 列名 | 型 | Excel 書式 | 備考 |
|---|------|----|-----------|------|
| 1 | 指番 | 文字列 | — | 台帳データ・工数データ双方に存在する指番 |
| 2 | 削除図形総数 | 整数 | `#,##0` | 台帳データ Summary 由来 |
| 3 | 追加図形総数 | 整数 | `#,##0` | 同上 |
| 4 | 変更図形総数 | 整数 | `#,##0` | 削除+追加 |
| 5 | 図形総数 | 整数 | `#,##0` | |
| 6 | 図形変更率[%] | 浮動小数点 | `0.00%` | fraction のまま保持、Excel側で%表示 |
| 7 | 差分ペア総数 | 整数 | `#,##0` | |
| 8 | 完全新規図面数 | 整数 | `#,##0` | |
| 9 | 指番図面総数 | 整数 | `#,##0` | |
| 10 | 流用率[%] | 浮動小数点 | `0.00%` | fraction のまま保持 |
| 11 | 新規作成率[%] | 浮動小数点 | `0.00%` | fraction のまま保持 |
| 12 | 電気設計要因不具合対応工数[h] | 浮動小数点 | `#,##0.00` | 工数データ由来 |
| 13 | サプライヤー要因不具合対応工数[h] | 浮動小数点 | `#,##0.00` | 同上 |
| 14 | 不具合対応工数[h] | 浮動小数点 | `#,##0.00` | 12+13の合計 |
| 15 | 図形変更効率[図形変更数/h] | 浮動小数点 | `#,##0.00` | 4÷14、14=0の場合は空白 |
| 16 | 図面変更作業効率[図番数/h] | 浮動小数点 | `#,##0.00` | 7÷14、14=0の場合は空白 |

書式:
- ヘッダー行: 青背景（#4472C4）・白文字・太字
- 先頭行固定（freeze_panes）
- 千の位区切り（`#,##0` 系書式）を整数・時間・効率列に適用

### ブラウザ表示（st.dataframe）

`pandas.Styler` を使用し、Excel と同一の書式でブラウザ上に表示する。

| 列種別 | Python フォーマット | 表示例 |
|--------|-------------------|--------|
| 整数列 | `"{:,}"` | `106,201` |
| 時間[h]列 | `"{:,.2f}"` | `39.42` |
| 効率列 | `"{:,.2f}"` | `29,572.00` |
| %列（fraction） | `"{:.2%}"` | `15.63%` |
| NaN | `""` （空白） | |

> `st.column_config.NumberColumn` は千の位区切りをサポートしないため、`result.style.format(DISPLAY_FORMATS, na_rep="")` を使用する。

---

## 定数・設定値

`app.py` 冒頭の定数を変更することで主要な動作を調整できる。

```python
UF1_TARGET   = "不具合対応"      # USER_FIELD_01 のフィルタ条件
UF2_ELEC     = "電気設計要因"    # USER_FIELD_02 の集計対象1
UF2_SUPPLIER = "サプライヤー要因"  # USER_FIELD_02 の集計対象2

LEDGER_SUMMARY_COLS  # 台帳データ Summary シートから読み込む列名のリスト（順序が出力列と対応）
OUT_COLS             # 最終出力の列順（16列）
INT_COLS / HOUR_COLS / EFFICIENCY_COLS / PERCENT_COLS  # 列を型カテゴリ別に分類したリスト
                        # （COL_FORMATS・DISPLAY_FORMATS はこれらから辞書内包表記で自動生成される）
COL_WIDTHS           # 出力列名 → Excel 列幅（dict）
```

---

## 依存パッケージ

| パッケージ | 役割 |
|-----------|------|
| `streamlit >= 1.40.0` | UI フレームワーク |
| `pandas >= 2.0.0` | データ処理・Styler によるブラウザ表示書式 |
| `openpyxl >= 3.0.0` | Excel 読み込み |
| `xlsxwriter >= 3.0.0` | Excel 書き込み・書式設定 |
| `plotly >= 5.20.0` | 流用率・不具合対応工数・不具合対応効率グラフの描画 |

---

## Streamlit Cloud へのデプロイ

1. このディレクトリを GitHub リポジトリにプッシュ
2. [Streamlit Cloud](https://share.streamlit.io/) でリポジトリを接続
3. Main file: `Design-productivity/app.py`（リポジトリルートからの相対パス）
4. `requirements.txt` が自動インストールされる

ローカル実行:

```bash
cd Design-productivity
pip install -r requirements.txt
streamlit run app.py
```

---

## 保守上の注意点

### USER_FIELD の条件を変更したい場合

`app.py` の定数 `UF1_TARGET` / `UF2_ELEC` / `UF2_SUPPLIER` を変更する。

### 台帳データの列名・シート構成が変わった場合

`LEDGER_SUMMARY_COLS` と `load_ledger()` 内の列名マッピングを更新する。
出力列を追加・変更する場合は `OUT_COLS` と `INT_COLS`/`HOUR_COLS`/`EFFICIENCY_COLS`/`PERCENT_COLS`
のいずれかへの追加、および `COL_WIDTHS` の更新が必要（`COL_FORMATS`/`DISPLAY_FORMATS` は
上記4リストから自動生成されるため個別更新は不要）。

### WBS要素(代入) 等の欠損値を文字列化する際の注意

`df[col].astype(str)` は欠損値（NaN）を **文字列の `"nan"`** に変換してしまう
（本物の欠損として扱われなくなる）。`load_efforts()` では `notna()` でマスクしてから
該当行のみ `astype(str)` している。指番のような結合キー列を文字列正規化する処理を
追加する際は、欠損値を先に除外するか `.where()` で非欠損行のみ変換すること。

### グラフのホバー・軸表記を変更したい場合

`make_reuse_rate_chart` / `make_recovery_effort_chart` / `make_recovery_effectiveness_charts`
内の `hovertemplate` と `yaxis_tickformat` を変更する（[d3-format](https://github.com/d3/d3-format) 構文）。

### Streamlit の `width` パラメータ

`use_container_width=True/False` は 2025-12-31 に削除予定。  
`width='stretch'` / `width='content'` を使用すること。

---

## 改訂履歴

| 日付 | 変更内容 |
|------|----------|
| 2026-05-20 | 初版作成 |
| 2026-05-20 | 出力列を11列に変更。列10を差分要素割合[%]（差分/合計×100）、列11を差分要素数／不具合対応工数[h]（差分/工数）に変更。列名を全て日本語化。千の位区切りを Excel・ブラウザ表示の両方に適用。 |
| 2026-08-03 | 台帳データ・工数データの入力形式変更に追随。台帳データは `merged_ledger.xlsx`（`Merged Data` シート、Child/Parent 重複排除）から `統合図面管理台帳.xlsx`（`Summary` シート、指番ごとに集計済み）に切り替え、重複排除ロジックを廃止。結合方式を OUTER JOIN から INNER JOIN に変更（台帳・工数双方に存在する指番のみ出力）。出力列を16列に拡張し、流用率・不具合対応工数・不具合対応効率の Plotly グラフ3種を追加（Excel出力はデータ表のみ）。 |
